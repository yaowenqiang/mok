apiVersion: v1
kind: Pod
metadata:
    name: my-pod-with-configmap
spec:
    containers:
      - name: my-service-with-config
        image: nginx:1.3.12
        env:
          - name: some value
            valueFrom:
                configMapKeyRef:
                    name: my-configmap
                    key: someVariable
